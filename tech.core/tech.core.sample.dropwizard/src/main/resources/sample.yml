# Register model/service/exception
# There are 2 ways to bind your resources.
#   1. Run from APIApplication.main() -- Register using following attributes will load appropriate
#      model/service classes on boot time, and enables to speak V3 API.
#         For (1) to work, service should implement interfaces defined under com.appirio.tech.core.api.v3.service package
#   2. Run from custom main() -- Alternatively, you can create you own custom Application and Configuration
#      classes of DropWizard, and implement V3 API by yourself.
v3services: 
  - com.appirio.tech.core.sample.services.UserCRUDService
  - com.appirio.tech.core.service.identity.services.UserService
  - com.appirio.tech.core.service.identity.services.CredentialService
# End registration of V3 API framework

database:
  # the name of your JDBC driver
  driverClass: com.informix.jdbc.IfxDriver

  # the username
  user: coder

  # the password
  password: 1nf0rm1x

  # the JDBC URL
  url: jdbc:informix-sqli://oltp01.prod.topcoder.com:2020/informixoltp:INFORMIXSERVER=informixoltp_tcp
  #url: jdbc:informix-sqli://localhost:7995/common_oltp:INFORMIXSERVER=informixoltp_tcp

  validationQuery: SELECT TODAY FROM systables WHERE tabid = 1

server:
  applicationConnectors:
    - type: http
      port: 8080
  adminConnectors:
    - type: http
      port: 8081

# Logging settings.
logging:
  # The default level of all loggers. Can be OFF, ERROR, WARN, INFO, DEBUG, TRACE, or ALL.
  level: INFO
  # Logger-specific levels.
  loggers:
    # Sets the level for 'com.appirio.tech' to DEBUG.
    com.appirio.tech: DEBUG
  appenders:
    - type: console
